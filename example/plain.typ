#set page(
  width: 3.5in,
  height: 1in,
  margin: (x:1mm, y:1mm),
)
#{
  v(1fr)

  set text(font: "Crimson Text", size: 12pt)
  [Digitally signed on #datetime.today().display("[day]-[month repr:short]-[year]") by:]

  v(1fr)

  text(weight: 800, size: 32pt, [Skeeters, Chad W])

  v(1fr)
}
